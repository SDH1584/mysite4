<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="gallery">

	<select id="galleryList" resultType="GalleryVo">
	<![CDATA[
			select ga.no no,
			       ga.user_no userNo,
			       us.name name,
			       content content,
			       saveName,
			       fileSize 
			from gallery ga, users us
            where ga.user_no = us.no
		
			
	]]>
	</select>

	<select id="readImg" parameterType="int" resultType="com.javaex.vo.GalleryVo">
   		 	<![CDATA[
		   	select no,
			       user_no userNo,
			       content,
			       saveName
			from gallery
			where no = #{no}
		]]>
	</select>

	<insert id="insImg" parameterType="GalleryVo">
	<![CDATA[
   			insert into gallery
			values(seq_gallery_no.nextval, #{userNo}, #{content}, #{filePath}, #{orgName}, #{saveName}, #{fileSize})
   		]]>
	</insert>

	<delete id="delImg" parameterType="int">
	<![CDATA[
		delete from gallery
		where no = #{no}
	]]>
	</delete>




</mapper>